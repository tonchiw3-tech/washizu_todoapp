# これから作るアプリの仕様書

## 1. このアプリでできること

やることを登録して管理するアプリです。できることは6つです。

| # | できること | 中身 |
|---|---|---|
| 1 | 一覧 | 登録したやることを、1画面に並べて出す |
| 2 | 登録 | 新しいやることを1件入れる |
| 3 | 編集 | 登録したやることを直す |
| 4 | 削除 | いらなくなったやることを消す |
| 5 | 検索 | やることの名前で、一覧を絞り込む |
| 6 | 並び替え | 期限の順に、一覧を並べ替える |

使う人は自分1人です。ログインの画面は作りません。

---

## 2. 保存するもの

保存する場所は、MySQLの中の **`todos`** という名前のテーブル1つです。1件のやることが1行になります。

| 列の名前 | 何を入れるか | 型 | 空でもよいか |
|---|---|---|---|
| `id` | 1件を見分ける番号 | `BIGINT` | いけない（自動で1ずつ増える・**主キー**） |
| `title` | やること | `VARCHAR(255)` | **いけない** |
| `detail` | メモ | `VARCHAR(255)` | よい |
| `category` | ジャンル | `VARCHAR(255)` | **いけない** |
| `priority` | 優先度（1＝高・2＝中・3＝低） | `INT` | いけない（既定は 2） |
| `due_date` | 期限 | `DATE` | よい |
| `completed` | 済みかどうか | `BOOLEAN` | いけない（既定は 未完了） |
| `created_at` | 登録した日時 | `DATETIME` | いけない（**MySQLが自動で入れる**） |
| `updated_at` | 最後に直した日時 | `DATETIME` | いけない（**MySQLが自動で入れる**） |

### ジャンルの5つ

`category` に入るのは、次の5つのどれかです。自由に打ち込む欄ではなく、選ぶ欄にします。

**デザイン ／ マーケティング ／ プログラミング ／ 資格 ／ 就職活動**

### 自分では入力しない列

`id`・`created_at`・`updated_at` の3つは、入力欄に出しません。保存のときに自動で値が入ります。

---

## 3. 作る画面は6枚

| 画面 | アドレス（`http://localhost:8080` に続く部分） | 何をする画面か |
|---|---|---|
| 一覧 | `/todos` | 登録したやることを並べる。アプリの中心 |
| 登録 | `/todos/new` | 新しい1件を入力する |
| 登録の確認 | `/todos/confirm` | 入れた内容を見せて、登録するかを選ばせる |
| 編集 | `/todos/{id}/edit` | 1件を呼び出して直す |
| 編集の確認 | `/todos/{id}/confirm` | 直した内容を見せて、保存するかを選ばせる |
| 削除確認 | `/todos/{id}/delete` | 消す1件を見せて、消すかを選ばせる |

`{id}` のところには、その1件の番号が入ります。

このほかに、トップページ（`/`）が1枚あります。カード7で作ったものをそのまま使います。

### 画面から画面への行き来

同じ画面に戻る操作（一覧の絞り込みと並び替え）は、この図に描いていません。

```
一覧 ──「新規登録」──→ 登録 ──「確認する」──→ 登録の確認 ──「登録する」──→ 一覧
                          ↑                        │
                          └────「書き直す」────┘

一覧 ──行の「編集」──→ 編集 ──「確認する」──→ 編集の確認 ──「保存する」──→ 一覧
                          ↑                        │
                          └────「書き直す」────┘

一覧 ──行の「削除」──→ 削除確認 ──「消す」──→ 一覧
                            └──「やめる」──→ 一覧
```

登録と編集の画面からは「一覧へ」で、削除確認の画面からは「やめる」で、一覧に戻れます。

---

## 4. 画面ごとの中身

### 一覧 `/todos`

**出すもの**（左から順に）

やること ／ ジャンル ／ 優先度 ／ 期限 ／ 状態 ／ 操作（編集・削除）

**上に置くもの**

- やることの名前で絞り込む入力欄
- ジャンルで絞り込む選ぶ欄（「すべて」を選ぶと絞り込まない）
- 「絞り込む」のボタン
- 並び替えのリンク2本（**期限が近い順** ／ **期限が遠い順**）
- 「新規登録」のボタン

**画面に出す言葉と、保存されている値は別のものです。**

| 保存されている値 | 画面に出す言葉 |
|---|---|
| `priority` の `1` / `2` / `3` | 高 / 中 / 低 |
| `completed` の `1` / `0` | 完了 / 未完了 |

**1件も登録が無いとき**は「まだ1件も登録されていません」と出します。

**画面のいちばん上**に、直前の操作の結果を1行出します（5-3. 操作のあとに出す文）。

押せるもの＝「絞り込む」／並び替えのリンク2本（期限が近い順・期限が遠い順）／「新規登録」／行ごとの「編集」「削除」

### 登録 `/todos/new`

| 入力欄 | 必ず入れるか | 欄の形 |
|---|---|---|
| やること | **必ず入れる** | 1行の文字 |
| メモ | 入れなくてよい | 1行の文字 |
| ジャンル | **必ず入れる** | 5つから選ぶ（最初は空欄） |
| 優先度 | **必ず入れる** | 高・中・低 から選ぶ（最初は空欄） |
| 期限 | 入れなくてよい | 日付 |

押せるもの＝「確認する」／「一覧へ」

### 登録の確認 `/todos/confirm`

入力された5つを、**入力欄ではなく文字として**並べます。入れなかった欄は「（入力なし）」と出します。

押せるもの＝「登録する」／「書き直す」（入れた値が入ったまま登録の画面に戻る）

### 編集 `/todos/{id}/edit`

登録の5つに、**完了（チェックを入れる欄）** を足した6つです。開いたときに、いまの値が入った状態で出します。必ず入れるのは、登録と同じ3つ（やること・ジャンル・優先度）です。

押せるもの＝「確認する」／「一覧へ」

### 編集の確認 `/todos/{id}/confirm`

登録の確認と同じ形です。押せるもの＝「保存する」／「書き直す」

### 削除確認 `/todos/{id}/delete`

消す1件の **やること・ジャンル・期限** を出します。入力欄はありません。

押せるもの＝「消す」／「やめる」

---

## 5. 決まりごと

### 5-1. 入力のきまり

必ず入れる欄が空のときは、保存せずに入力の画面に留まり、その欄の下に文を出します。

| 欄 | 止めるとき | 画面に出す文 |
|---|---|---|
| やること | 空のまま | やることを入力してください |
| やること | 255文字を超えた | やることは255文字以内で入力してください |
| メモ | 255文字を超えた | メモは255文字以内で入力してください |
| ジャンル | 選ばれていない | ジャンルを選んでください |
| 優先度 | 選ばれていない | 優先度を選んでください |

期限には決まりを付けません。

**空白だけを入れて送っても止まります。**半角の空白でも、全角の空白（日本語入力をオンにしたまま押した `スペース`）でも、中身が空のあつかいになります。

### 5-2. 存在しない番号を開いたとき

編集や削除確認のアドレスに、テーブルに無い番号を入れて開いたときは、**一覧に戻し**、いちばん上に「見つかりませんでした」と出します。専用の画面は作りません。

### 5-3. 操作のあとに出す文

一覧のいちばん上に、直前の操作の結果を1行出します。

| いつ | 出す文 |
|---|---|
| 登録したあと | 登録しました |
| 編集を保存したあと | 保存しました |
| 削除したあと | 削除しました |
| 無い番号を開いたとき | 見つかりませんでした |

### 5-4. 検索と並び替え

| | 決めたこと |
|---|---|
| 名前での絞り込み | `title` に、入れた言葉が**一部でも含まれていれば**残す |
| ジャンルでの絞り込み | `category` が**ぴったり同じ**ものだけ残す |
| 並び替え | `due_date` の順。**最初に開いたときは、期限が近い順** |
| どちらも空のとき | 絞り込まずに全部出す |
| 絞り込みの結果が0件のとき | 一覧の「まだ1件も登録されていません」と同じ文を出す（0件専用の文は増やさない） |

絞り込みの言葉と並びの向きは、**アドレスの後ろに付けて送ります**（例：`/todos?keyword=課題&category=プログラミング&order=asc`）。こうすると、絞り込んだ画面のアドレスをそのまま開き直せます。

### 5-5. 記録（ログ）に残すもの

登録・編集・削除の3つが動いたときに、1行ずつ記録を残します。

- **残す**＝何の操作か（登録／編集／削除）と、その1件の `id`
- **残さない**＝`title` と `detail` の中身

---

## 6. 画面の見た目

見た目は**CSSを1枚だけ**作り、6枚の画面すべてで読み込みます。画面ごとに見た目を変えることはしません。

### 6-1. 置く場所と読み込み方

| | 決めたこと |
|---|---|
| ファイル | `src/main/resources/static/css/style.css`（**1枚だけ**） |
| 読み込み方 | 各テンプレートの `<head>` に `<link rel="stylesheet" href="/css/style.css">` を1行 |
| 増やさないもの | 画面の中に直接書く `style="..."` ／ CSSファイルの2枚目 |

`static/` は雛形（カード5で作ったもの）にありますが、**その下の `css/` はまだありません**。カード7で作ります。

### 6-2. 使う名前（class）

**この名前だけを使います。** 新しい名前を足しません。画面に何かを足すときも、下の表にある名前から選びます。

| どこ | class名 | 使う画面 |
|---|---|---|
| 画面全体の入れ物 | `container` | 6枚すべて |
| 画面のタイトル | `page-title` | 6枚すべて |
| 一覧の表 | `todo-table` | 一覧 |
| 直前の操作の結果を出す1行 | `flash` | 一覧 |
| 絞り込みと並び替えの帯 | `search-bar` | 一覧 |
| 入力欄1行（ラベルと欄の組） | `form-row` | 登録・編集 |
| ラベル | `form-label` | 登録・編集 |
| 入力欄・選ぶ欄 | `form-input` | 登録・編集 |
| 入力が止まったときの文 | `error` | 登録・編集 |
| 確認画面に並べる値 | `confirm-value` | 登録の確認・編集の確認・削除確認 |
| 確認画面のボタンの並び | `confirm-actions` | 登録の確認・編集の確認・削除確認 |
| ボタン | `btn` | 6枚すべて |
| ボタン（副＝書き直す・やめる・一覧へ） | `btn sub` | 6枚すべて |
| 文字のリンク（編集・削除・並び替え） | `link` | 一覧 |

### 6-3. CSS

`src/main/resources/static/css/style.css` の中身は、次のとおりです。**この1枚だけ**を使い、増やしません。

```css
* {
    box-sizing: border-box;
}

body {
    margin: 0;
    color: #334155;
    background: #ffffff;
    font-family: sans-serif;
    font-size: 16px;
    line-height: 1.6;
}

.container {
    width: 100%;
    max-width: 900px;
    margin: 0 auto;
    padding: 24px 16px;
}

.page-title {
    margin: 0 0 24px;
    color: #1e293b;
    font-size: 28px;
}

.todo-table {
    width: 100%;
    border-collapse: collapse;
}

.todo-table th,
.todo-table td {
    border: 1px solid #94a3b8;
    padding: 6px 10px;
    text-align: left;
    font-size: 14px;
}

.todo-table th {
    background: #f1f5f9;
    font-weight: bold;
}

.flash {
    margin: 0 0 16px;
    border: 1px solid #cbd5e1;
    border-radius: 8px;
    padding: 8px 12px;
    background: #f1f5f9;
}

.search-bar {
    display: flex;
    flex-wrap: wrap;
    align-items: end;
    gap: 12px;
    margin-bottom: 20px;
}

.form-row {
    margin-bottom: 16px;
}

.form-label {
    display: block;
    margin-bottom: 4px;
}

.form-input {
    width: 100%;
    border: 1px solid #94a3b8;
    border-radius: 6px;
    padding: 7px 10px;
    background: #ffffff;
    color: #334155;
    font: inherit;
}

.search-bar .form-input {
    width: auto;
}

.error {
    margin: 4px 0 0;
    color: #B91C1C;
    font-weight: bold;
}

.confirm-value {
    margin: 0 0 16px;
}

.confirm-actions {
    display: flex;
    gap: 12px;
}

.btn {
    display: inline-block;
    border: 1px solid #334155;
    border-radius: 7px;
    padding: 7px 18px;
    background: #334155;
    color: #ffffff;
    font: inherit;
    text-decoration: none;
    cursor: pointer;
}

.btn.sub {
    background: #ffffff;
    color: #334155;
}

.link {
    color: #334155;
    text-decoration: underline;
}
```

**飾りはこれだけです。** 影・動き・画像・外から読み込むCSSは使いません。

---

## 7. この仕様書の使いかた

| いつ | 何に使うか |
|---|---|
| カード7 | 6. をCodexへの依頼文に書き写し、`style.css` を1枚作る |
| カード8〜11 | この仕様書を読み、**Codexに3枚の設計書を作らせて、その中身をこの仕様書と突き合わせて点検する** |
| カード12〜17 | Codexへの依頼文に、この仕様書の値をそのまま書き写す（**6-2. のclass名も毎回書く**） |
| カード18 | できあがった実物と、この仕様書を1行ずつ突き合わせる |

**この仕様書に書いていないことは、今回は作りません。**思いついた機能は、25枚を作りきってから足します。

---

## 8. 今回は作らないもの

- 一覧のページ分け（件数が増えたときに何ページかに分ける仕組み）
- 消したものを後から戻せる仕組み
- 完了にした日時の記録
- ログインと、複数の人での共有
- 写真やファイルの添付
