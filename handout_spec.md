# これから足すものの仕様書

> **これは、フレームワーク演習で配った仕様書に「足すもの」を書いたものです。**
> 前の仕様書は捨てません。2枚を並べて使います。画面は **6枚 → 7枚** になります。

---

## 1. 何を足すのか

いまのアプリは、**画面からしか使えません**。ここに口を3つ足します。

| 足すもの | できるようになること |
|---|---|
| API | 画面以外のプログラムから、同じデータを読み書きできる |
| 外部との連携 | その日が祝日かどうかが分かる |
| カレンダー画面 | 期限を、表ではなく暦で見られる |
| MCP | AIに話しかけて、やることを動かせる |

**このアプリが何をするものになるか**

> **締め切りを落とさないための、自分専用のタスク管理アプリ**

足す4つは、全部この1つのためにあります。

---

## 2. 変えないもの

| 対象 | |
|---|---|
| `todos` テーブル | **列を1つも足しません。変えません** |
| いまの6画面 | **画面の作り（列・並び順・入力欄）は変えません。** カレンダーとの行き来のリンクだけ足します |
| CSS | 1枚のままです |
| Java・Spring Boot | 21・4.0.7 のままです |

---

## 3. API

### 3-1. 作るAPIは6つ

`http://localhost:8080` に続く部分です。

| # | 方法 | アドレス | 何をするか | うまくいったとき |
|---|---|---|---|---|
| 1 | GET | `/api/todos` | 一覧を返す | 200 |
| 2 | GET | `/api/todos/{id}` | 1件を返す | 200 |
| 3 | POST | `/api/todos` | 1件登録する | 201 |
| 4 | PUT | `/api/todos/{id}` | 1件直す | 200 |
| 5 | DELETE | `/api/todos/{id}` | 1件消す | 204 |
| 6 | GET | `/api/holidays` | 祝日を返す | 200 |

### 3-2. 方法（メソッド）の決まり

| 方法 | 使うとき | 決まり |
|---|---|---|
| GET | 取ってくるだけ | **中身を変えてはいけません** |
| POST | 新しく作る | |
| PUT | 中身を入れ替える | 全部の項目を送ります |
| DELETE | 消す | |

**「GETは中身を変えない」は世界共通の決まりです**（RFC 9110 という文書）。
フレームワーク演習で削除をPOSTにしたのは、この決まりのためでした。

### 3-3. 番号（ステータスコード）の決まり

| 番号 | 意味 | いつ返すか |
|---|---|---|
| 200 | できた | 取れた・直せた |
| 201 | 作れた | 登録できた。**どこに作ったかを `Location` で返します** |
| 204 | できた（返すものは無い） | 消せた |
| 400 | 送られた中身がおかしい | 入力の決まりを満たしていない |
| 404 | 見つからない | その番号が無い |

### 3-4. `/api/todos` に付けられる条件

アドレスの後ろに付けます。例＝`/api/todos?category=プログラミング&order=asc`

| 名前 | 入れる値 | 何もしないとき |
|---|---|---|
| `keyword` | やることの名前の一部 | 絞り込まない |
| `category` | ジャンル（5つのどれか） | 絞り込まない |
| `order` | `asc`（期限が近い順）／`desc`（遠い順） | `asc` |
| `from` `to` | 期限の範囲（`2026-10-01` の形） | 絞り込まない |

`from` と `to` は、カレンダー画面が使います。

**日本語を条件に入れるとき**
アドレスに日本語はそのまま書けないため、「%E9%9D%A2%E6%8E%A5」のような形に変換してから送ります。
ブラウザも REST Client も自動で変換するので、手で変換する場面はありません。

### 3-5. 返す中身の形

```json
{
  "id": 2,
  "title": "競合サイトを3つ調べる",
  "detail": null,
  "category": "マーケティング",
  "priority": 2,
  "dueDate": null,
  "completed": false,
  "createdAt": "2026-04-01T09:15:07",
  "updatedAt": "2026-04-01T09:15:07"
}
```

**「高」「完了」などの言葉は返しません。** 保存されている値（`1` や `false`）だけを返します。
どんな言葉で出すかは、**受け取った側が決めます**。
前の仕様書の「保存されている値と、画面に出す言葉は別」と同じ考え方です。

**名前の付け方が変わります**＝データベースは `due_date`、APIは `dueDate`。区切りが「_」から「大文字」に変わります。これも世界共通の慣習です。

### 3-6. うまくいかなかったときに返す形

決まった形が1つあります（RFC 9457 という文書の形）。

```json
{
  "type": "about:blank",
  "title": "Bad Request",
  "status": 400,
  "detail": "入力に誤りがあります",
  "instance": "/api/todos",
  "errors": [
    { "field": "title",    "message": "やることを入力してください" },
    { "field": "category", "message": "ジャンルを選んでください" }
  ]
}
```

`errors` に出す文は、**前の仕様書の 5-1 と同じ文**を使います。新しく作りません。

---

## 4. 祝日を取ってくる

| | 決めたこと |
|---|---|
| どこから | `https://holidays-jp.github.io/api/v1/date.json` |
| 鍵（APIキー） | **要りません** |
| お金 | **かかりません** |
| 返ってくる形 | `{"2026-01-01": "元日", "2026-01-12": "成人の日", ...}` |

**外に取りに行くのは、自分のアプリです。** カレンダー画面から直接ではなく、`/api/holidays` を通します。
こうすると、外のサービスの都合（アドレスや形が変わる）を**1か所に閉じ込められます**。

**取りに行けなかったときも、画面は出します。** 祝日の色が付かないだけにして、画面の上に1行「祝日を取れませんでした」と出します。

---

## 5. カレンダー画面

| | 決めたこと |
|---|---|
| アドレス | `/calendar` |
| 出すもの | 1か月の升目。その日が期限のやることを入れる |
| 1マスに出す数 | **3件まで**。それより多いときは「ほか N件」 |
| 移動 | 前の月・次の月 |
| 色を変える日 | 土曜・日曜・祝日 |
| 赤くするもの | 期限が過ぎていて、まだ完了していないもの |

**升目はアプリが作り、中身はブラウザが取ってきます。**
ブラウザから `/api/todos?from=&to=` と `/api/holidays?from=&to=` を呼び、返ってきた中身を升目に入れます。

**ここが、自分で作ったAPIを最初に使う場面です。**

---

## 6. MCP

### 6-1. MCPで何ができるか

AI（Codex）に話しかけると、AIがあなたのアプリを操作します。

- 「来週のやることを教えて」→ 一覧を読む
- 「木曜に歯医者を入れて」→ 1件足す
- 「来週の空いてる日に動かして」→ 空き日を探して、期限を変える

**動かしたあとにカレンダーを開き直すと、変わっています。**

### 6-2. 作るツールは7つ

| ツールの名前 | 何をするか |
|---|---|
| `list_todos` | やることの一覧を返す（期間・ジャンルで絞れる） |
| `get_todo` | やることを1件返す |
| `add_todo` | やることを1件足す |
| `update_todo` | やることを1件直す（期限を変えるのもこれ） |
| `complete_todo` | やることを完了にする |
| `delete_todo` | やることを1件消す |
| `find_free_days` | 期間の中で、期限のやることが無く、土日でも祝日でもない「空いている日」を返す。やることの期限を動かす先を決めるのに使う |

### 6-3. 作り方

| | |
|---|---|
| 足すもの | 依存を1つ（`spring-ai-starter-mcp-server-webmvc`） |
| 設定 | `application.properties` に `spring.ai.mcp.server.protocol=STREAMABLE` を1行。**書かないと窓口のアドレスが `/mcp` になりません** |
| 書くこと | メソッドに `@McpTool` を付ける。引数に `@McpToolParam` を付ける |
| 省略できる引数 | **`required = false` を必ず書く**（書かないと必須になり、AIの呼び出しが失敗します） |
| 口 | `http://localhost:8080/mcp` |
| 別のプログラム | **要りません。**いまのアプリがそのまま口を持ちます |

**説明文（`description`）が、AIの正確さを決めます。** 何をするツールなのかを、AIが読んで分かる言葉で書きます。

---

## 7. 今回は作らないもの

- APIを使うときの合言葉（APIキー・ログイン）※危なさの話はします
- 祝日をデータベースに保存しておく仕組み
- カレンダーの上でドラッグして予定を動かす
- `todos` テーブルへの列の追加
- 一覧のページ分け

**この仕様書に書いていないことは、今回は作りません。** 思いついたものは、Day5以降の発展課題で足します。

---

## 8. この仕様書の使いかた

| いつ | 何に使うか |
|---|---|
| Day1 | 3. をCodexへの依頼文に書き写して、APIを作る |
| Day2 | 4. を書き写して、祝日を取ってくる |
| Day3 | 5. を書き写して、カレンダー画面を作る |
| Day4 | 6. を書き写して、MCPの口を作る |
