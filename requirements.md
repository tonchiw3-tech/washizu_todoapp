# 要件定義書

## 1. 文書の目的

本書は、やること（Todo）を登録して管理するアプリの要件（このアプリが満たすべき条件）を定めるものです。

本書は `handout_app_spec.md` をもとに整理しています。データベースの詳しい構造はDB設計書、画面の詳しい配置や見た目は画面設計書で定めます。

### 1.1 叩き台からの追加

読み手が要件を確認しやすいよう、次の文書上の整理を追加しています。

- 要件に番号を付けています。
- 「対象利用者」「前提・制約」「受け入れ条件（完成したと判断する条件）」の見出しを設けています。
- 本書で使う専門用語に短い説明を添えています。
- 名前とジャンルを同時に指定した場合は、両方の条件に合うものを残すと明文化しています。

これらはアプリに新しい機能を追加するものではありません。

## 2. 対象範囲

### 2.1 対象利用者

- 利用者は1人です。
- ログイン（利用者を確認する画面）は設けません。

### 2.2 対象とする機能

アプリは、次の6つの機能を提供します。

| 要件ID | 機能 | 内容 |
|---|---|---|
| FR-01 | 一覧 | 登録されているやることを一覧で確認できる。 |
| FR-02 | 登録 | 新しいやることを1件登録できる。 |
| FR-03 | 編集 | 登録済みのやることを変更できる。 |
| FR-04 | 削除 | 登録済みのやることを削除できる。 |
| FR-05 | 検索 | やることの名前とジャンルで一覧を絞り込める。 |
| FR-06 | 並び替え | 期限が近い順または遠い順に一覧を並べ替えられる。 |

### 2.3 対象外

次の機能は今回の対象外です。

- 一覧のページ分け（件数を複数ページに分ける機能）
- 削除したデータを元に戻す機能
- 完了にした日時を記録する機能
- ログイン、複数利用者での共有
- 写真やファイルの添付

## 3. 前提・制約

- アプリは `http://localhost:8080` で利用します。
- データはMySQL（データを保存するソフト）の `todos` テーブルに保存します。
- 1件のやることを、データベースの1行として扱います。
- データベースの詳しい項目、データ型、主キー（1件を一意に見分ける番号）などはDB設計書で定めます。
- 画面の詳しい配置、色、文字の大きさ、CSS（画面の見た目を指定する仕組み）などは画面設計書で定めます。
- 画面全体で使用するCSSファイルは1枚とし、画面ごとに別の見た目は作りません。

## 4. 管理する情報

### 4.1 やること1件の情報

アプリは、1件のやることについて次の情報を扱います。

| 情報 | 内容 | 利用者の入力 |
|---|---|---|
| やること | 実施する内容 | 登録・編集で入力する |
| メモ | やることに関する補足 | 登録・編集で入力する。省略できる |
| ジャンル | やることの分類 | 登録・編集で5種類から選ぶ |
| 優先度 | やることの重要度 | 登録・編集で「高・中・低」から選ぶ |
| 期限 | やることの期限 | 登録・編集で入力する。省略できる |
| 完了状態 | 完了したかどうか | 編集でチェックを入れて変更する |

次の情報はシステム（アプリまたはデータベース）が管理し、利用者の入力欄には表示しません。

- やることを見分ける番号（ID）
- 登録日時
- 最終更新日時

### 4.2 ジャンル

ジャンルは自由入力ではなく、次の5種類から選択します。

- デザイン
- マーケティング
- プログラミング
- 資格
- 就職活動

### 4.3 初期値と表示名

- 優先度を選んでいない状態は登録できません。保存時の既定値（指定がない場合の値）は「中」です。
- 完了状態の既定値は「未完了」です。
- 保存値 `1`・`2`・`3` は、それぞれ画面上で「高」・「中」・「低」と表示します。
- 保存値の完了・未完了は、画面上で「完了」・「未完了」と表示します。

## 5. 機能要件

### 5.1 一覧表示（FR-01）

- 登録済みのやることを一覧で表示します。
- 各件について、やること、ジャンル、優先度、期限、状態、操作（編集・削除）を表示します。
- 1件もない場合、または絞り込み結果が0件の場合は、「まだ1件も登録されていません」と表示します。
- 一覧を最初に開いたときは、期限が近い順に表示します。
- 一覧から、新規登録、編集、削除、絞り込み、並び替えを開始できます。

### 5.2 登録（FR-02）

- 利用者は、やること、メモ、ジャンル、優先度、期限を入力または選択できます。
- 登録前に確認画面を表示します。
- 確認画面では、入力内容を入力欄ではなく文字として表示します。
- 未入力の項目は「（入力なし）」と表示します。
- 利用者が「登録する」を選んだ場合だけ、1件を保存します。
- 登録後は一覧に戻り、「登録しました」と表示します。
- 「書き直す」を選んだ場合は、入力内容を保持したまま登録画面に戻ります。
- 登録画面から「一覧へ」を選ぶと、保存せず一覧に戻ります。

### 5.3 編集（FR-03）

- 利用者は一覧から1件を選び、その内容を編集画面で変更できます。
- 編集画面を開いたときは、現在の値を入力欄に表示します。
- 編集では、登録時の5項目に加えて完了状態を変更できます。
- 編集前に確認画面を表示します。
- 利用者が「保存する」を選んだ場合だけ、変更内容を保存します。
- 保存後は一覧に戻り、「保存しました」と表示します。
- 「書き直す」を選んだ場合は、変更内容を保持したまま編集画面に戻ります。
- 編集画面から「一覧へ」を選ぶと、変更せず一覧に戻ります。

### 5.4 削除（FR-04）

- 利用者は一覧から1件を選び、削除確認画面を表示できます。
- 削除確認画面では、削除対象のやること、ジャンル、期限を表示します。
- 削除確認画面に入力欄は設けません。
- 利用者が「消す」を選んだ場合だけ、対象の1件を削除します。
- 削除後は一覧に戻り、「削除しました」と表示します。
- 「やめる」を選んだ場合は、削除せず一覧に戻ります。

### 5.5 絞り込み（FR-05）

- やることの名前を入力して絞り込めます。
- 入力した言葉が、やることの名前に一部でも含まれるものを残します。
- ジャンルを選んで絞り込めます。
- 「すべて」を選んだ場合は、ジャンルで絞り込みません。
- やることの名前とジャンルの両方を指定した場合は、両方の条件に合うものを残します。
- 条件を指定しない場合は、すべての件を表示します。
- 絞り込み条件はURL（画面の場所を表すアドレス）の後ろに付けて送信し、絞り込み後のアドレスを開き直しても条件を再現できるようにします。

### 5.6 並び替え（FR-06）

- 一覧から「期限が近い順」と「期限が遠い順」を選択できます。
- 並び替えは期限の順で行います。
- 並びの向きはURLの後ろに付けて送信し、並び替え後のアドレスを開き直しても再現できるようにします。

### 5.7 存在しないデータへの対応

- 編集または削除確認で、存在しない番号を指定した場合は専用画面を表示しません。
- 一覧に戻り、「見つかりませんでした」と表示します。

### 5.8 操作記録

- 登録、編集、削除が成功したとき、それぞれ1行のログ（操作の記録）を残します。
- ログには操作の種類（登録・編集・削除）と対象のIDを残します。
- やることとメモの内容はログに残しません。

## 6. 入力・エラー要件

### 6.1 必須項目

次の項目は必須（必ず入力または選択する項目）です。

- やること
- ジャンル
- 優先度

メモと期限は省略できます。

### 6.2 入力チェック

入力チェック（保存前に内容を確認する処理）で問題が見つかった場合は保存せず、入力画面に留まります。該当する欄の下に次の文を表示します。

| 項目 | 条件 | 表示する文 |
|---|---|---|
| やること | 未入力、または空白だけ | やることを入力してください |
| やること | 255文字を超える | やることは255文字以内で入力してください |
| メモ | 255文字を超える | メモは255文字以内で入力してください |
| ジャンル | 未選択 | ジャンルを選んでください |
| 優先度 | 未選択 | 優先度を選んでください |

- 半角スペースだけでなく、全角スペースだけの場合も空白だけとして扱います。
- 期限には入力チェックの決まりを設けません。

## 7. 画面・画面遷移要件

### 7.1 必要な画面

次の画面を用意します。`{id}` は対象データの番号に置き換わります。

| 画面 | URLの末尾 | 役割 |
|---|---|---|
| トップページ | `/` | 既存のトップページを使用する。 |
| 一覧 | `/todos` | 登録済みのやることを確認・操作する。 |
| 登録 | `/todos/new` | 新しいやることを入力する。 |
| 登録確認 | `/todos/confirm` | 登録内容を確認し、登録または修正を選ぶ。 |
| 編集 | `/todos/{id}/edit` | 指定したやることを変更する。 |
| 編集確認 | `/todos/{id}/confirm` | 変更内容を確認し、保存または修正を選ぶ。 |
| 削除確認 | `/todos/{id}/delete` | 指定したやることを確認し、削除または中止を選ぶ。 |

### 7.2 主な画面遷移

- 一覧 → 登録 → 登録確認 → 一覧
- 登録確認 → 登録（「書き直す」）
- 一覧 → 編集 → 編集確認 → 一覧
- 編集確認 → 編集（「書き直す」）
- 一覧 → 削除確認 → 一覧
- 登録・編集から「一覧へ」で一覧に戻る
- 削除確認から「やめる」で一覧に戻る

各画面の項目配置、ボタンの位置、見た目、CSSのクラス名は画面設計書で定めます。

## 8. 受け入れ条件

受け入れ条件（要件を満たしたと判断する確認項目）は、次のとおりです。

- やることを登録・一覧表示・編集・削除できる。
- 登録と編集で、確認画面を経由してから保存できる。
- 必須項目の未入力、空白だけの入力、文字数超過を指定された文で知らせ、保存しない。
- やることの名前とジャンルで一覧を絞り込める。
- 期限の近い順と遠い順に一覧を並べ替えられる。
- 絞り込み条件と並び順を、アドレスを開き直しても再現できる。
- 存在しない番号を指定した場合、一覧に戻って「見つかりませんでした」と表示する。
- 登録・編集・削除の成功時に、操作の種類と対象IDだけがログに残る。
- 対象外に定めた機能を実装しない。

## 9. 参照元

- `handout_app_spec.md`（本書作成時点の仕様の叩き台）
