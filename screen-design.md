# 画面設計書

## 1. 文書の目的

本書は、やること管理アプリの画面の配置、画面に出す言葉、入力時のエラー、画面間の移動方法を定めるものです。

「URL」は画面の場所を表すアドレス、「ID」はやること1件を見分ける番号です。

## 2. 対象画面と前提

### 2.1 対象画面

新たに設計する画面は、次の6つだけです。画面を増やしません。

| 画面ID | 画面名 | URL | 役割 |
|---|---|---|---|
| S-01 | 一覧 | `/todos` | 登録済みのやることを確認・検索・並び替えする |
| S-02 | 登録 | `/todos/new` | 新しいやることを入力する |
| S-03 | 登録確認 | `/todos/confirm` | 登録前に入力内容を確認する |
| S-04 | 編集 | `/todos/{id}/edit` | 指定したやることを変更する |
| S-05 | 編集確認 | `/todos/{id}/confirm` | 保存前に変更内容を確認する |
| S-06 | 削除確認 | `/todos/{id}/delete` | 指定したやることを削除する前に確認する |

`{id}` は、対象となるやることの番号に置き換えます。例えばIDが `12` の場合、編集画面は `/todos/12/edit` です。

トップページ（`/`）は既存のものを使用するため、本書で新しい画面として設計する対象には含めません。

### 2.2 共通の決まり

- 利用者は1人で、ログイン画面は作りません。
- 6画面すべてに、画面全体の入れ物として `container`、画面タイトルとして `page-title` を使います。`class` は画面の部品を見分ける名前です。
- 見た目を指定するCSS（画面の色や配置を指定する仕組み）は `/css/style.css` だけを読み込みます。
- CSSの具体的な指定は `handout_app_spec.md` の6-3をそのまま採用します。色、余白、文字サイズ、装飾、その他6-3に記載されている画面表示上のルールも含みます。
- ボタンには `btn`、補助的なボタン（戻る・取り消し）には `btn sub`、文字のリンクには `link` を使います。
- `id`、登録日時、最終更新日時は画面の入力欄に出しません。
- 必須項目（必ず入力または選択する項目）は「やること」「ジャンル」「優先度」です。
- 任意項目（入力しなくてもよい項目）は「メモ」「期限」です。
- 入力のない値は、確認画面では `（入力なし）` と表示します。

## 3. 画面遷移

画面遷移（ある画面から別の画面へ移ること）は次のとおりです。

```text
S-01 一覧
  ├─「新規登録」────────→ S-02 登録
  │                          ├─「確認する」→ S-03 登録確認
  │                          │                 ├─「登録する」→ S-01 一覧
  │                          │                 └─「書き直す」→ S-02 登録
  │                          └─「一覧へ」──────────────────→ S-01 一覧
  │
  ├─行の「編集」──────────→ S-04 編集
  │                          ├─「確認する」→ S-05 編集確認
  │                          │                 ├─「保存する」→ S-01 一覧
  │                          │                 └─「書き直す」→ S-04 編集
  │                          └─「一覧へ」──────────────────→ S-01 一覧
  │
  ├─行の「削除」──────────→ S-06 削除確認
  │                          ├─「消す」────→ S-01 一覧
  │                          └─「やめる」──→ S-01 一覧
  │
  ├─「絞り込む」──────────→ S-01 一覧（条件をURLに付けて再表示）
  ├─「期限が近い順」──────→ S-01 一覧（期限の近い順で再表示）
  └─「期限が遠い順」──────→ S-01 一覧（期限の遠い順で再表示）
```

### 3.1 URLに付ける検索・並び替え条件

絞り込みや並び替えの条件は、URLの後ろに付けて送ります。これをクエリ（URLに付ける条件）といいます。

例：`/todos?keyword=課題&category=プログラミング&order=asc`

| 条件 | URLの名前 | 値 |
|---|---|---|
| やることの名前 | `keyword` | 入力された文字。名前の一部でも含めば表示 |
| ジャンル | `category` | 選択されたジャンル。`すべて` は条件なし |
| 並び順 | `order` | `asc` は期限が近い順、`desc` は期限が遠い順 |

名前とジャンルを両方指定した場合は、両方の条件に合うものだけを表示します。条件がない場合はすべて表示します。

## 4. 共通の表示ルール

### 4.1 保存値と表示する言葉

データベース（データを保存する仕組み）に保存された値は、次の言葉に置き換えて表示します。

| 保存値 | 画面に出す言葉 |
|---|---|
| 優先度 `1` | 高 |
| 優先度 `2` | 中 |
| 優先度 `3` | 低 |
| 完了状態 `0` または `FALSE` | 未完了 |
| 完了状態 `1` または `TRUE` | 完了 |

ジャンルの選択肢は、次の5つに固定します。自由入力欄にはしません。

`デザイン` ／ `マーケティング` ／ `プログラミング` ／ `資格` ／ `就職活動`

### 4.2 ボタン・リンクの共通表記

| 種類 | 表示する言葉 | 動作 |
|---|---|---|
| 主ボタン | 確認する | 入力内容を確認画面へ送る |
| 主ボタン | 登録する | 登録確認の内容を保存して一覧へ戻る |
| 主ボタン | 保存する | 編集確認の内容を保存して一覧へ戻る |
| 主ボタン | 消す | 削除確認の対象を削除して一覧へ戻る |
| 副ボタン | 書き直す | 入力内容を保持して入力画面へ戻る |
| 副ボタン | 一覧へ | 保存せず一覧へ戻る |
| 副ボタン | やめる | 削除せず一覧へ戻る |

## 5. 画面詳細

### 5.1 S-01 一覧

#### 基本情報

| 項目 | 内容 |
|---|---|
| 画面タイトル | 一覧 |
| URL | `/todos` |
| 使用する主なclass | `container`, `page-title`, `flash`, `search-bar`, `todo-table`, `btn`, `link` |
| 初期表示 | 期限が近い順。期限がないものは期限があるものの後ろ |

#### 上部の表示と操作

 直前の操作結果を、画面タイトル「一覧」より上（画面のいちばん上）に1行表示します。結果がない場合は表示しません。

| 部品 | 表示する言葉・値 | 動作 |
|---|---|---|
| 操作結果 | 登録しました／保存しました／削除しました／見つかりませんでした | 一覧の上部に表示 |
| 名前入力欄のラベル | やること | `title` の一部一致で絞り込む |
| 名前入力欄 | URLの `keyword` の値 | 直前の検索条件があれば保持 |
| ジャンル選択欄のラベル | ジャンル | `category` で完全一致の絞り込み |
| ジャンル選択肢 | すべて、デザイン、マーケティング、プログラミング、資格、就職活動 | 「すべて」はジャンル条件なし |
| 絞り込みボタン | 絞り込む | 指定条件で一覧を再表示 |
| 並び替えリンク | 期限が近い順／期限が遠い順 | `order=asc`／`order=desc` で再表示 |
| 登録ボタン | 新規登録 | S-02 登録へ移動 |

#### 一覧表

表の見出しは左から次の順にします。

| 列の見出し | 表示内容 |
|---|---|
| やること | `title` |
| ジャンル | `category` |
| 優先度 | 「高」「中」「低」のいずれか |
| 期限 | `due_date`。未入力は `（入力なし）` |
| 状態 | 「完了」または「未完了」 |
| 操作 | 行ごとに「編集」「削除」 |

「編集」は対象IDの S-04 編集へ、「削除」は対象IDの S-06 削除確認へ移動します。

対象が0件の場合は、表の代わりに次の文を表示します。

`まだ1件も登録されていません`

これは登録データが0件の場合だけでなく、絞り込み結果が0件の場合にも表示します。

### 5.2 S-02 登録

#### 基本情報

| 項目 | 内容 |
|---|---|
| 画面タイトル | 登録 |
| URL | `/todos/new` |
| 使用する主なclass | `container`, `page-title`, `form-row`, `form-label`, `form-input`, `error`, `btn` |

#### 入力項目

上から次の順に表示します。ラベル（入力欄の名前）を入力欄の上に置き、エラーがある場合はその欄の下に表示します。

| 順 | ラベル | 入力の形 | 必須 | 初期状態 |
|---:|---|---|---|---|
| 1 | やること | 1行の文字入力 | 必須 | 空欄 |
| 2 | メモ | 1行の文字入力 | 任意 | 空欄 |
| 3 | ジャンル | 選択欄 | 必須 | 未選択 |
| 4 | 優先度 | 「高」「中」「低」の選択欄 | 必須 | 未選択 |
| 5 | 期限 | 日付入力欄 | 任意 | 空欄 |

#### 操作

- 「確認する」：入力チェックを行い、問題がなければ S-03 登録確認へ移動します。
- 「一覧へ」：保存せず S-01 一覧へ戻ります。

入力チェックで問題がある場合は保存せず、この画面に留まり、入力内容を保持します。

### 5.3 S-03 登録確認

#### 基本情報

| 項目 | 内容 |
|---|---|
| 画面タイトル | 登録確認 |
| URL | `/todos/confirm` |
| 使用する主なclass | `container`, `page-title`, `confirm-value`, `confirm-actions`, `btn` |

#### 表示項目

入力欄は表示せず、入力された値を文字として次の順に表示します。

`やること` ／ `メモ` ／ `ジャンル` ／ `優先度` ／ `期限`

メモまたは期限が未入力の場合は、その値を `（入力なし）` と表示します。優先度は「高」「中」「低」と表示します。

#### 操作

- 「登録する」：1件を保存し、S-01 一覧へ移動します。一覧の上部に `登録しました` と表示します。
- 「書き直す」：入力値を保持したまま S-02 登録へ戻ります。

### 5.4 S-04 編集

#### 基本情報

| 項目 | 内容 |
|---|---|
| 画面タイトル | 編集 |
| URL | `/todos/{id}/edit` |
| 使用する主なclass | `container`, `page-title`, `form-row`, `form-label`, `form-input`, `error`, `btn` |

#### 入力項目

対象の現在値を入力欄に入れた状態で表示します。上から次の順に表示します。

| 順 | ラベル | 入力の形 | 必須 |
|---:|---|---|---|
| 1 | やること | 1行の文字入力 | 必須 |
| 2 | メモ | 1行の文字入力 | 任意 |
| 3 | ジャンル | 5種類の選択欄 | 必須 |
| 4 | 優先度 | 「高」「中」「低」の選択欄 | 必須 |
| 5 | 期限 | 日付入力欄 | 任意 |
| 6 | 完了 | チェックを入れる欄 | 入力不要（現在値を表示） |

チェックが入っている場合は完了、入っていない場合は未完了として表示します。チェックなしも有効な状態として保存します。

#### 操作

- 「確認する」：入力チェックを行い、問題がなければ S-05 編集確認へ移動します。
- 「一覧へ」：変更を保存せず S-01 一覧へ戻ります。

入力チェックで問題がある場合は保存せず、この画面に留まり、入力内容を保持します。

存在しないIDの場合は、この画面を表示せず S-01 一覧へ戻り、上部に `見つかりませんでした` と表示します。

### 5.5 S-05 編集確認

#### 基本情報

| 項目 | 内容 |
|---|---|
| 画面タイトル | 編集確認 |
| URL | `/todos/{id}/confirm` |
| 使用する主なclass | `container`, `page-title`, `confirm-value`, `confirm-actions`, `btn` |

#### 表示項目

入力欄は表示せず、変更後の値を文字として次の順に表示します。

`やること` ／ `メモ` ／ `ジャンル` ／ `優先度` ／ `期限` ／ `完了`

メモまたは期限が未入力の場合は `（入力なし）` と表示します。完了状態は `完了` または `未完了` と表示します。

#### 操作

- 「保存する」：変更内容を保存し、S-01 一覧へ移動します。一覧の上部に `保存しました` と表示します。
- 「書き直す」：変更値を保持したまま S-04 編集へ戻ります。

対象のIDが存在しない場合は、確認画面を表示せず S-01 一覧へ戻り、上部に `見つかりませんでした` と表示します。

### 5.6 S-06 削除確認

#### 基本情報

| 項目 | 内容 |
|---|---|
| 画面タイトル | 削除確認 |
| URL | `/todos/{id}/delete` |
| 使用する主なclass | `container`, `page-title`, `confirm-value`, `confirm-actions`, `btn` |

#### 表示項目

入力欄は置かず、削除する対象の次の3項目を文字として表示します。

`やること` ／ `ジャンル` ／ `期限`

期限が未入力の場合は `（入力なし）` と表示します。

#### 操作

- 「消す」：対象を削除し、S-01 一覧へ移動します。一覧の上部に `削除しました` と表示します。
- 「やめる」：削除せず S-01 一覧へ戻ります。

対象のIDが存在しない場合は、確認画面を表示せず S-01 一覧へ戻り、上部に `見つかりませんでした` と表示します。

## 6. 入力エラーの表示

入力チェック（保存前に入力内容を確認する処理）で問題が見つかった場合は、保存せず入力画面に留まります。エラー文は該当する入力欄の下に、赤い文字で表示します。

| 画面 | 項目 | 条件 | 表示するエラー文 |
|---|---|---|---|
| S-02 登録、S-04 編集 | やること | 未入力または半角・全角スペースだけ | やることを入力してください |
| S-02 登録、S-04 編集 | やること | 255文字を超えた | やることは255文字以内で入力してください |
| S-02 登録、S-04 編集 | メモ | 255文字を超えた | メモは255文字以内で入力してください |
| S-02 登録、S-04 編集 | ジャンル | 未選択 | ジャンルを選んでください |
| S-02 登録、S-04 編集 | 優先度 | 未選択 | 優先度を選んでください |

期限には入力チェックの決まりを設けません。複数の項目に問題がある場合は、該当するすべての欄の下にエラー文を表示します。

## 7. 操作後の表示

登録・編集・削除が成功した場合、または存在しないIDを開いた場合は、一覧の最上部に次の1行を表示します。これをフラッシュメッセージ（直前の操作結果を一時的に知らせる文）とします。

| 操作 | 表示する文 |
|---|---|
| 登録成功 | 登録しました |
| 編集の保存成功 | 保存しました |
| 削除成功 | 削除しました |
| 存在しないID | 見つかりませんでした |

フラッシュメッセージの表示部分には `flash`、一覧の上部に置きます。

## 8. 画面部品とclass名の対応

画面部品のclass名（CSSが部品を見分けるための名前）は、次の既定名だけを使います。

| 画面部品 | class名 | 使用画面 |
|---|---|---|
| 画面全体の入れ物 | `container` | 6画面すべて |
| 画面タイトル | `page-title` | 6画面すべて |
| 一覧表 | `todo-table` | S-01 |
| 操作結果の1行 | `flash` | S-01 |
| 検索・並び替えの帯 | `search-bar` | S-01 |
| 入力欄とラベルの組 | `form-row` | S-02、S-04 |
| 入力欄のラベル | `form-label` | S-02、S-04 |
| 入力欄・選択欄 | `form-input` | S-02、S-04 |
| 入力エラー文 | `error` | S-02、S-04 |
| 確認画面の表示値 | `confirm-value` | S-03、S-05、S-06 |
| 確認画面のボタンの並び | `confirm-actions` | S-03、S-05、S-06 |
| ボタン | `btn` | 6画面すべて |
| 副ボタン | `btn sub` | 6画面すべて |
| 文字のリンク | `link` | S-01 |

## 9. 画面数の確認

本書で設計する画面は、次の6画面です。

1. 一覧
2. 登録
3. 登録確認
4. 編集
5. 編集確認
6. 削除確認

検索、並び替え、入力エラー、存在しないIDへの対応は、別画面を増やさず、S-01・S-02・S-04・S-05・S-06の中で行います。

## 10. 見出し一覧

1. 文書の目的
2. 対象画面と前提
   1. 対象画面
   2. 共通の決まり
3. 画面遷移
   1. URLに付ける検索・並び替え条件
4. 共通の表示ルール
   1. 保存値と表示する言葉
   2. ボタン・リンクの共通表記
5. 画面詳細
   1. S-01 一覧
   2. S-02 登録
   3. S-03 登録確認
   4. S-04 編集
   5. S-05 編集確認
   6. S-06 削除確認
6. 入力エラーの表示
7. 操作後の表示
8. 画面部品とclass名の対応
9. 画面数の確認
10. 見出し一覧
