<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.todoapp.TodoMapper">

    <select id="search" resultType="com.example.todoapp.Todo">
        SELECT id, title, detail, category, priority, due_date, completed,
               created_at, updated_at
        FROM todos
        <where>
            <if test="keyword != null and keyword != ''">
                AND title LIKE CONCAT('%', #{keyword}, '%')
            </if>
            <if test="category != null and category != ''">
                AND category = #{category}
            </if>
        </where>
        ORDER BY due_date IS NULL,
        <choose>
            <when test="order == 'desc'">
                due_date DESC, id DESC
            </when>
            <otherwise>
                due_date ASC, id ASC
            </otherwise>
        </choose>
    </select>

    <select id="findById" resultType="com.example.todoapp.Todo">
        SELECT id, title, detail, category, priority, due_date, completed,
               created_at, updated_at
        FROM todos
        WHERE id = #{id}
    </select>

    <insert id="insert">
        INSERT INTO todos (title, detail, category, priority, due_date)
        VALUES (#{title}, #{detail}, #{category}, #{priority}, #{dueDate})
    </insert>

    <update id="update">
        UPDATE todos
        SET title = #{title},
            detail = #{detail},
            category = #{category},
            priority = #{priority},
            due_date = #{dueDate},
            completed = #{completed}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM todos
        WHERE id = #{id}
    </delete>

</mapper>
